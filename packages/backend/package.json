{
  "name": "@next-relay-playground/backend",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler build",
    "start": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "lint": "biome check",
    "format": "biome check --write",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/graphql-server": "^0.5.1",
    "graphql": "^16.10.0",
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.101.0"
  }
}
